# TEMPLATE — não aplique este arquivo diretamente em produção.
#
# Este manifesto documenta as chaves esperadas pelo Secret "backend-endereco-secrets"
# referenciado em api-deployment.yaml, postgres-deployment.yaml e monitoring-deployment.yaml.
# Os valores abaixo são placeholders em base64 ("changeme"), não segredos reais.
#
# Para criar o Secret real, prefira um destes caminhos (fora do controle de versão):
#   kubectl create secret generic backend-endereco-secrets \
#     --from-literal=db-user=<usuario-real> \
#     --from-literal=db-password=<senha-real> \
#     --from-literal=jwt-secret=<segredo-jwt-real> \
#     --from-literal=grafana-admin-password=<senha-real>
# ou um cofre de segredos (Vault, Sealed Secrets, External Secrets Operator etc).
apiVersion: v1
kind: Secret
metadata:
  name: backend-endereco-secrets
type: Opaque
data:
  db-user: Y2hhbmdlbWU=
  db-password: Y2hhbmdlbWU=
  jwt-secret: Y2hhbmdlbWU=
  grafana-admin-password: Y2hhbmdlbWU=
