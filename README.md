# backend-endereco
Serviço de backend para CRUD de endereços com rotas protegidas (JWT). Implementa sistema de logs para operações críticas e endpoints de acesso temporário não autenticado.  Tags/Topics para adicionar ao repositório: nodejs express postgresql jwt-authentication docker rest-api backend-challenge
